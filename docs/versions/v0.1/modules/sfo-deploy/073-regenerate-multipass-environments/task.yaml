schema_version: 1

# 用户已确认提案并选择 standard 层级。
workflow_tier: standard

version: v0.1
packet_module: sfo-deploy
task_name: 073-regenerate-multipass-environments
stage: proposal
mode: manual
auto_pipeline_start_stage:

proposal: proposal.md
completion_report: completion-report.md
change_record: docs/changes/073-regenerate-multipass-environments.md
design:
testing:
testplan:
acceptance_report:
pipeline_plan:
risk_profile:
lifecycle_state:

changed_paths_file: .harness/evidence/v0.1/stage-scope/073-regenerate-multipass-environments.paths
baseline_manifest: .harness/baselines/v0.1/073-regenerate-multipass-environments-delivery/manifest.json

changes:
  - id: CHG-regenerate-multipass-environments
    target_module: sfo-deploy
    scope_paths: ["examples/eleph-server-multipass/cluster-template/environments/**", "examples/eleph-server-multipass/README.md"]
    changed_paths_file:
