schema_version: 1

# 每个任务创建此清单和 proposal.md，并在用户确认前保持 pending。
workflow_tier: high-risk

# 规范任务身份；其他任务文档引用此文件，不再重复这些值。
version: v0.1
packet_module: sfo-deploy
task_name: 031-install-deno-ensure-tools
stage: acceptance
mode: manual
auto_pipeline_start_stage:

# proposal 是所有层级共用的基线；其余路径在高风险确认后才会物化。
proposal: proposal.md
completion_report: completion-report.md
change_record:
design: design.md
testing: testing.md
testplan: testplan.yaml
acceptance_report: acceptance-report.md
pipeline_plan: pipeline/plan.md
risk_profile: risk-profile.yaml
lifecycle_state: lifecycle.json

changed_paths_file: .harness/evidence/v0.1/stage-scope/031-install-deno-ensure-tools.paths
baseline_manifest:

changes:
  - id: CHG-deno-ensure-tools
    target_module: sfo-deploy
    scope_paths: ["src/ssh_install.ts", "tests/unit/ssh_install.test.ts", "tests/dv/install_deno.test.ts", "README.md", "docs/guides/sfo-deploy-cluster-configuration.md", "examples/eleph-server-multipass/README.md"]
    changed_paths_file:
  - id: CHG-deno-ensure-tools-docs
    target_module: sfo-deploy
    scope_paths: ["README.md", "docs/guides/sfo-deploy-cluster-configuration.md", "examples/eleph-server-multipass/README.md"]
    changed_paths_file:
