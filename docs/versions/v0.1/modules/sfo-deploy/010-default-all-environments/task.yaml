schema_version: 1

# 每个任务创建此清单和 proposal.md，并在用户确认前保持 pending。
workflow_tier: standard

# 规范任务身份；其他任务文档引用此文件，不再重复这些值。
version: v0.1
packet_module: sfo-deploy
task_name: 010-default-all-environments
stage: proposal
mode: manual
auto_pipeline_start_stage:

# proposal 是所有层级共用的基线；其余路径在高风险确认后才会物化。
proposal: proposal.md
# trivial/standard 通过 completion-report 收尾。
completion_report: completion-report.md
change_record: docs/changes/010-default-all-environments.md
design: design.md
testing: testing.md
testplan: testplan.yaml
acceptance_report: acceptance-report.md
pipeline_plan: pipeline/plan.md
risk_profile: risk-profile.yaml
lifecycle_state: lifecycle.json

changed_paths_file: .harness/evidence/v0.1/stage-scope/010-default-all-environments.paths
baseline_manifest: .harness/baselines/v0.1/010-default-all-environments-delivery/manifest.json

# 变更范围按变更关联；其余全部元数据由 task.yaml 承担。
changes:
  - id: CHG-default-all-environments
    target_module: sfo-deploy
    scope_paths: ["src/sfo_deploy/**", "tests/**", "README.md"]
    changed_paths_file:
