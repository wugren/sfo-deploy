---
Task manifest: task.yaml
---

## Object and Scope

- Task manifest: task.yaml
- Review mode: independent（验收负责人未参与实现与测试，按
  proposal/design/实现代码/测试代码与运行制品重新独立审查，并以自建探针在真实 buildDeploymentBundle
  → updateConfig 链路上复核）
- Scope: sfo-deploy 模块 048-text-placeholder-updater 的三个 change_id（`management.configs` 新增
  `type: template` 文本占位符 updater 的装载/生成控制面、远端 marker
  全量替换运行面、文档与测试收敛）

## Findings

| id   | severity | owning_stage | correctness_category | evidence                                                                                                                                                                                                         | problem                                                                                                                                                                                                                                                                                                | blocking |
| ---- | -------- | ------------ | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| F-01 | none     | none         | none                 | docs/changes/047-remove-v1-config-layouts.md:60-64 仍将三项先存失败测试（packageless_app_scripts ×2、secrets_deploy_config）登记为后续跟进                                                                       | 该历史变更记录是 047 收尾时的事实快照；本任务实现与测试阶段已按当前生命周期语义修复/移除这三项并使全仓 `deno task test` 232 过/0 败（testing.md Validation Rationale 已说明），历史记录文本不回写属正常快照语义，活文档无过时表述                                                                      | no       |
| F-02 | none     | none         | none                 | tests/unit/deployment_bundle.test.ts 的 bindings.json 断言覆盖 yaml/json/toml/ini/script 五种 updaterKind；template 骨架 → 真实 bundle 成员序列化无专属自动化用例，集成测试以本地 bindingDocument 复刻序列化形态 | 验收自建探针已在真实链路（buildDeploymentBundle 打包 → 解 tar → 远端 updateConfig）复核：emitted bindings 为 `{secret, kind: "value", encoding: "utf8", type: "string", selector: null, marker}` 且被远端接受、全量替换成功；TS 类型系统同时固定该形态。属覆盖面观察项，可作为后续用例补充，非交付缺陷 | no       |
| F-03 | none     | none         | none                 | docs/guides/sfo-deploy-cluster-configuration.md 旧二进制快照边界段明确「旧版本二进制无法解码含 template 的新快照，回滚需用同版本二进制」；仓库无历史二进制制品可供自动化回归                                     | 旧二进制兼容边界仅由文档声明，无法在 CI 内以真实旧二进制验证；编码侧 JSON 显式 `{kind: "template", bindings}` 新形态由 tests/unit/history.test.ts 往返断言固定，风险为单向（新读旧）且已在设计/文档中说明                                                                                              | no       |

## Requirement Coverage

| change_id                       | requirement_or_boundary                                                                                                                                                                                            | source            | implementation_evidence                                                                                                                                                                                                                                                                                                                                                                                                                                          | finding    | status |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------ |
| CHG-template-updater-control    | app.yaml schema v3 `management.configs[].updater.type: template`：`${NAME}` 占位符绑定（缺省=秘密名、重复拒绝）、仅值秘密、与 variables 互斥、声明未使用/保留前缀/非法名称失败关闭；控制端生成不含秘密的确定性骨架 | proposal.md P-001 | src/config.ts:938-968 managedTemplateBindings（SECRET_RE/CONFIG_VARIABLE_RE/重复/declaredManagedSecret(secret,"value")）；src/config.ts:1017 template 分支与 variables 互斥文案；src/config_generation.ts:22 TEMPLATE_PLACEHOLDER_RE、:145 templateBindings（marker 派生）、:171-215 renderTemplateSkeleton（`$$`/字面 `$`/未声明/缺席/保留前缀全分支）；tests/unit/app_management_config.test.ts 与 managed_config_generation.test.ts 逐分支断言错误文案        | 无阻断发现 | pass   |
| CHG-template-updater-remote     | 远端 config_updater 支持 `--format template`：marker 全量替换（缺失即失败）、原文注入不转义、残留 marker/UTF-8/大小校验、deny env/net/run/FFI 不变、bundle 离线执行且重新编译提交                                  | proposal.md P-002 | src/remote_runtime/config_updater.ts:80 replaceAllMarkers 接线、:85 残留守卫、:120 format === "template" 分支（selector null/kind value/encoding utf8/type string 门禁）、:163-167 count 0 →「秘密 marker 必须至少出现一次」、validateRendered 跳过结构化复解析；src/remote_runtime/config_updater.bundle.js 重新生成（52726 字节，`deno task bundle:remote-runtime` 复核字节一致）；tests/integration/config_updater.test.ts 端到端断言                         | 无阻断发现 | pass   |
| CHG-template-updater-docs-tests | 快照 encode/decode 携带 template 绑定、计划/CLI 秘密集合归一为值秘密；指南与 README template 语法/示例与实现一致并由 contract 校验                                                                                 | proposal.md P-003 | src/history.ts encode/decodeConfigUpdater template 分支（v4 往返，tests/unit/history.test.ts）；src/planning.ts managedSecrets 与 src/cli.ts serializeManagement 归一（config_planning/cli_human_output 用例）；README.md:258-265 与 docs/guides/sfo-deploy-cluster-configuration.md:529-560 template 段落含 `${DB_PASSWORD}` 示例；tests/contract/verify_app_management_contract.ts 断言 guide 含 `type: template`/`${DB_PASSWORD}`、readme 含 `type: template` | 无阻断发现 | pass   |

## Independent Defect Discovery

| category                      | applicable_scope                                                                                                            | evidence_inspected                                                                                                                                                                                                                                                                                                                           | adversarial_check                                                                                                                                                                                                                                                                                                                                                               | finding_or_not_applicable_reason                                                                             | status         |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------- |
| requirement-and-behavior      | 三个 change_id 与 proposal Confirmed 边界（仅 `${NAME}` + `$$`、无裸 `$NAME`、仅值秘密、与 variables 互斥、失败关闭不发布） | proposal.md Scope/Non-goals；src/config.ts:938-968、src/config_generation.ts:171-215、src/remote_runtime/config_updater.ts:47-99/120/163；README.md:258-265；docs/guides/sfo-deploy-cluster-configuration.md:529-560                                                                                                                         | 自建探针实测：真实 generateConfigSkeleton → buildDeploymentBundle → updateConfig 全链路，50 次重复占位符 + `$$` + 字面 `$` + 含正则元字符/引号/换行/`${OTHER}`/Unicode 的秘密值逐字节符合预期且无 marker 残留；file 秘密绑定 template 在装载面被拒（「引用的 value 密钥 KEY_FILE 未在 App 顶层对应密钥列表声明」）；README/指南由 contract docs 模式断言关键表述                | 需求边界逐条落地，文档表述与实现行为一致，未发现缺失或越界行为                                               | pass           |
| logic-and-control-flow        | 装载/骨架生成/远端替换的控制流与错误出口                                                                                    | src/config.ts:1017（variables 互斥先于 template 分支）、src/config_generation.ts:79-84/145-215、src/remote_runtime/config_updater.ts:80-85/120-129/163-167                                                                                                                                                                                   | 探针复核替换为 split/join 单遍实现（无递归展开：秘密值内 `${OTHER}` 保持字面）；marker 缺失（count 0）即抛错、验证器跳过仅限 template 分支、既有 yaml/json/toml/ini/script 分支由 app_management_config 四格式回归与 deployment_bundle 用例逐字段锁定                                                                                                                           | 未发现错误分支、静默 fallthrough 或可达性缺陷                                                                | pass           |
| boundary-and-input            | 占位符名称取值域、marker 身份派生、秘密值字节形态边界                                                                       | src/config_generation.ts:22（`^[A-Z][A-Z0-9_]*$`）、:145-169 templateBindings、src/remote_runtime/config_updater.ts:163-167 全量计数                                                                                                                                                                                                         | 探针实测 51 处占位符（含连续逗号分隔与行尾无换行）全量替换；50 次重复同一占位符同 marker 一致；秘密值含 tab/换行/引号/反斜杠/多字节 UTF-8 注入后逐字节相等；单元用例覆盖小写/数字开头/空占位符拒收                                                                                                                                                                              | 取值域与字节边界 fail-closed 且无误报                                                                        | pass           |
| state-and-data-integrity      | 计划/历史快照往返一致性与源文件不被改写                                                                                     | src/history.ts encode/decodeConfigUpdater template 分支；tests/unit/history.test.ts v4 template 往返（deploy step 补 secretValues/deliveryInputs 断言）；tests/integration/config_updater.test.ts                                                                                                                                            | 探针确认 updateConfig 只写 output 不回写 input；template 快照 decode 逐字段一致且既有五种 updater 解码回归不变；guide 明确旧二进制不读新快照的回滚边界（F-03）                                                                                                                                                                                                                  | 无非法状态转换或持久化损坏路径                                                                               | pass           |
| error-handling-and-recovery   | template 全部失败出口（装载/生成/远端）                                                                                     | src/config.ts 装载错误文案断言（app_management_config 负例组）、src/config_generation.ts:205-215 保留前缀/未声明/缺席、src/remote_runtime/config_updater.ts:85/99/165                                                                                                                                                                        | 探针实测含 `__SFO_` 秘密在注入后被残留守卫拒绝（「配置候选仍包含秘密占位符」），错误信息不含秘密值或候选内容；远端失败不落候选不落 target（集成负例断言）                                                                                                                                                                                                                       | 错误分类、失败关闭与消息脱敏符合设计与文档                                                                   | pass           |
| resource-lifetime-and-cleanup | 本任务改动的文本处理路径（装载/骨架生成/纯字符串替换，无句柄、子进程、网络或定时器新增）                                    | src/config.ts:938-968、src/config_generation.ts:145-215、src/remote_runtime/config_updater.ts:120-167（均为纯函数式文本处理；远端临时目录沿用既有 updater 生命周期清理）                                                                                                                                                                     | not-applicable：template 分支不引入任何新资源生命周期；探针与集成用例的临时文件为验收/测试脚手架，非产品代码                                                                                                                                                                                                                                                                    | 本任务为纯文本装载/生成/替换分支，成功与失败路径均无新增句柄、后台任务或需清理的外部资源，无可断言的清理缺陷 | not-applicable |
| concurrency-and-ordering      | 本任务改动的路径（单次顺序执行，无共享可变状态或并发构造）                                                                  | src/remote_runtime/config_updater.ts template 分支在既有单遍 updateConfig 流程内顺序执行；src/config_generation.ts 为确定性纯函数（同输入同 marker，单元断言）                                                                                                                                                                               | not-applicable：无新增并发构造、锁或共享可变状态；替换顺序由既有绑定列表顺序决定且 marker 互异（不同绑定 marker 派生互异由单元断言），无顺序依赖可断言缺陷                                                                                                                                                                                                                      | 本任务未引入或删除任何并发构造与共享可变状态，marker 互异且全量替换与遍历顺序无关，无并发语义变化            | not-applicable |
| interface-and-compatibility   | 公开 TypeScript 导出、既有四种结构化/script updater 契约、bundle 协议消费者                                                 | src/mod.ts 仅新增类型导出；tests/unit/app_management_config.test.ts 四格式回归（装载逐字段不变）；tests/unit/deployment_bundle.test.ts 既有格式成员形态断言；tests/unit/history.test.ts 既有快照解码回归；`deno task check` 闭合（contract repository-compile-closure step exit 0）                                                          | 探针确认既有格式 bindings.json 成员形态不变；`--format template` 为新增可选值，parseArgs 收宽不影响旧参数；新旧快照单向兼容边界已文档化（F-03）                                                                                                                                                                                                                                 | 接口向后兼容面无回归，新增 template 为纯扩展                                                                 | pass           |
| security-and-capacity         | 秘密信任边界（不进 argv/env/stdout、仅从受限目录读取、marker 保留前缀、候选守卫）                                           | src/remote_runtime/config_updater.ts:85/99 残留守卫、:163-167、deny env/net/run/FFI 既有断言（integration 负例）；src/config_generation.ts:205 保留前缀拒收；探针输出                                                                                                                                                                        | 探针实测骨架内只有 marker（无秘密值）；含 `__SFO_` 前缀的秘密值注入后立即被守卫拒绝；绑定清单仅含 secret 名/类型/encoding/selector/marker，无值；CLI 序列化（serializeManagement）只含绑定元数据；错误消息经检查不含秘密或候选内容                                                                                                                                              | 信任面收窄且失败关闭，无注入面或泄露面新增                                                                   | pass           |
| test-adequacy                 | 单元/集成/契约三层与任务作用域统一入口                                                                                      | testplan.yaml（U1-U4、I1-I2、repository-compile-closure/documentation-examples/template-new-path-compiles 三类 contract step）；运行制品 .harness/test-results/test-runs/20260905T093558Z-sfo-deploy+048-text-placeholder-updater-all.json（7 步全部 exit 0）；testing.md 覆盖表与 risk-profile required_checks 映射（Validation Rationale） | 每类 required_check 均有反例：装载失败矩阵（重复/未声明/非法/variables 互斥）、骨架失败矩阵（`$$`/字面/未声明/缺席/保留前缀）、远端失败（marker 缺失/非法绑定/残留/UTF-8/大小）、快照往返、文档契约断言；验收自建探针独立于测试套件复现端到端正向与 `__SFO_` 负例；全仓 `deno task test` 232 过/0 败（含三项先存失败修复，F-01）；F-02 记录 bundle 级序列化的非阻断补充用例机会 | 覆盖充分且缺项有解释；测试能暴露相关缺陷类别                                                                 | pass           |

## Document Consistency

| document      | source                    | implementation_consistency                                                                                                         | finding | status |
| ------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------- | ------ |
| proposal.md   | proposal.md               | 已批准；P-001/002/003 与最终实现/测试/文档一致，Non-goals（不做结构化复解析、不改既有格式语义）逐项成立                            | 无      | pass   |
| design        | design.md                 | 占位符语法裁定、marker 派生、装载/生成/替换序列与实现一致；范围补录 src/cli.ts、src/planning.ts、src/mod.ts 后与实际改动路径闭合   | 无      | pass   |
| testing       | testing.md, testplan.yaml | 覆盖表与 testplan 步骤一致；运行制品可复核；DV disabled 理由与任务性质相符；三项先存失败处置有依据说明                             | 无      | pass   |
| testplan.yaml | testplan.yaml             | 三个 change_id 均有 Direct Change 映射；contract 三类 step 覆盖编译闭合/文档示例/template 编译路径；manual_gaps 为空且全仓测试全绿 | 无      | pass   |

## Result Summary

- Overall result: accepted
- Outcome: 独立缺陷审查覆盖 template updater
  的装载/骨架生成/远端替换/快照持久化/文档五条面；自建探针在真实 buildDeploymentBundle →
  updateConfig 链路复核（bundle 序列化形态、51 处全量替换、`$$` 与字面
  `$`、复杂秘密值逐字节、`__SFO_` 残留失败关闭、file 秘密装载拒收、bundle 产物字节同步）；任务作用域
  7 步运行制品全过且全仓 232 用例 0 失败；三条非阻断观察项（F-01 历史记录快照、F-02 bundle
  级序列化用例机会、F-03 旧二进制边界依赖文档声明）。
- Blocking issues: none
- Next action: 完成任务收尾（lifecycle 完成与变更记录交付），F-02 可作为后续测试补充项，F-03
  维持文档边界声明。

## Conclusion

- Accepted / rejected / needs changes: accepted
- Reason: 三个 change_id 的需求覆盖均有具体实现与运行证据；十类缺陷发现全部 pass 或有任务特定的
  not-applicable 理由；独立探针未发现任何可证伪的交付缺陷；文档一致性通过；无阻断发现。
