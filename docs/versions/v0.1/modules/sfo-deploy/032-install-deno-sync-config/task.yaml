schema_version: 1

# 每个任务创建此清单和 proposal.md，并在用户确认前保持 pending。
workflow_tier: high-risk

# 规范任务身份；其他任务文档引用此文件，不再重复这些值。
version: v0.1
packet_module: sfo-deploy
task_name: 032-install-deno-sync-config
stage: acceptance
mode: manual
auto_pipeline_start_stage:

# proposal 是所有层级共用的基线；其余路径在高风险确认后才会物化。
proposal: proposal.md
completion_report: completion-report.md
change_record:
design: design.md
testing: testing.md
testplan: testplan.yaml
acceptance_report: acceptance-report.md
pipeline_plan: pipeline/plan.md
risk_profile: risk-profile.yaml
lifecycle_state: lifecycle.json

changed_paths_file: .harness/evidence/v0.1/stage-scope/032-install-deno-sync-config.paths
baseline_manifest:

changes:
  - id: CHG-install-deno-sync-config
    target_module: sfo-deploy
    scope_paths: ["src/integration.ts", "src/config.ts", "src/ssh_install.ts", "src/results.ts", "src/mod.ts", "tests/unit/install_deno_cli.test.ts", "tests/dv/install_deno.test.ts", "tests/integration/install_deno_cli.test.ts"]
    changed_paths_file:
  - id: CHG-install-deno-sync-docs
    target_module: sfo-deploy
    scope_paths: ["README.md", "docs/guides/sfo-deploy-cluster-configuration.md", "examples/eleph-server-multipass/README.md", "tests/contract/verify_install_deno_contract.ts"]
    changed_paths_file:
