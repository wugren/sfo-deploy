# 独立远端生命周期脚本第二轮独立验收报告

## Findings

| ID         | Severity | Owning Stage | Correctness Category | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Problem                                                                                                                                                                 | Blocking |
| ---------- | -------- | ------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| A019-F-001 | none     | none         | test-adequacy        | `tests/dv/execution.test.ts:247-317` 构造非空秘密、模板、制品和 file-secret，并在 `477-632` 经真实 `prepareExecution`、`DeploymentExecutor.executePrepared` 精确断言 context、file-secret、0600/0700、上传/执行/删除/工作区清理顺序和 execute 参数；`636-677` 注入 context 上传失败并断言无执行、context 删除、workspace/session/local/artifact 清理；`tests/unit/transport_cli.test.ts:53-71` 直接断言 OpenSSH 实际发出 `chmod 0600`；本轮独立重跑 DV 7、transport unit 8、script integration 4 均通过 | 已关闭：第一轮指出的 executor→独立脚本普通输入桥接、有效 mode、严格顺序和失败清理缺口已有会在字段遗漏、错配、权限放宽或清理缺失时失败的具体回归；未发现新的阻断 finding | no       |

## Object and Scope

- Task manifest: task.yaml
- Review date: 2026-09-02
- In-scope implementation:
  `src/execution.ts`、`src/transport.ts`、`src/remote_context.ts`、`src/mod.ts`，模板与实际集群共四十二个生命周期脚本，README/配置指南，任务
  contract/unit/DV/integration 测试、当前 runtime state，以及
  `.harness/test-results/test-runs/20260901T160514Z-sfo-deploy+019-independent-remote-scripts-all.json`
- Review mode: independent falsification; conclusion selected after findings and category
  review；第二轮没有沿用第一轮结论，重新读取 proposal、plan、testplan、runtime
  state、生产调用链、当前测试断言和新制品，并独立运行针对性 DV/unit/integration/消费者闭包检查

## Requirement Coverage

| change_id                      | Requirement or Boundary                                                                                                                                    | Source                                              | Implementation Evidence                                                                                                                                                                                                                                                                                                                                                                                                                             | Finding                                                                                         | Status |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------ |
| CHG-independent-remote-scripts | 远端只上传声明的独立生命周期脚本和普通步骤数据，不生成、上传或导入框架源码；仓库脚本与说明迁移为自包含模型，同时保持秘密、模板、制品、权限、失败和清理边界 | `proposal.md` P-001、P-002、Scope、Success Criteria | `src/execution.ts:400-461` 仅预检解释器并投递 package/file-secret/template/context/声明脚本；`src/transport.ts:418-455` 以 `DEPLOYMENT_CONTEXT_PATH` 和固定 no-remote/no-npm/deny-ffi/run/net 权限启动 Deno；仓库扫描确认远端脚本无 framework import，模板与实际集群各 21 个脚本逐路径内容相同；新 artifact 的五类 contract、unit、DV、integration 共 8 步均 exit 0，本轮又独立运行 DV 7、transport unit 8、script integration 4、closure/docs 检查 | A019-F-001 的全部反例已有精确断言；零框架上传、普通输入消费、兼容边界和所需验证均未发现剩余阻断 | pass   |

## Independent Defect Discovery

| Category                      | Applicable Scope                                                                                             | Evidence Inspected                                                                                                                                        | Adversarial Check                                                                                                                                                       | Finding or Not-Applicable Reason                                                                                                                                                      | Status |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| requirement-and-behavior      | 零远端框架代码、独立脚本普通 JSON 输入、消费者全面迁移及旧 helper 兼容非目标                                 | `proposal.md` P-001/P-002、Scope/Out of scope/Success Criteria；`src/execution.ts:400-461`；四十二个生命周期脚本；三份公共说明                            | 搜索 `sfo_deploy.ts/.py`、`remote_runtime`、`DeploymentContext` 和 removed fields，区分控制端本地 API、远端消费者与迁移文字；检查旧 helper 快照是否被错误承诺为直接兼容 | 当前上传链不生成框架源码，远端脚本无框架 import；文档明确 helper-dependent 脚本/快照需迁移且不保证直接回放，没有发现需求矛盾、缩减或越出 non-goal 的行为                              | pass   |
| logic-and-control-flow        | `prepareExecution`、`executePrepared`、`#executeStep` 的预检、上传、逐脚本执行、fail-fast 与终态             | `src/execution.ts:74-192,237-529`；`tests/dv/execution.test.ts:319-677`；新 artifact DV step                                                              | 挑战重复步骤、混合运行时、每机预检复用、依赖阻断、单机失败、跨目标隔离、context 上传失败及 helper 重新出现                                                              | 控制流在上传失败时不执行脚本并进入 remove/workspace/session 清理；普通与非空输入 DV 锁定预检、上传、执行和清理序列，未发现错误 fallthrough 或成功误报                                 | pass   |
| boundary-and-input            | schema v1 context、秘密名/值、模板映射、package/action、路径、mode 与远端 argv                               | 六个 context 消费脚本；`src/remote_context.ts:16-155`；`src/transport.ts:282-305,418-455,639-735`；script integration 4 项                                | 尝试缺失环境变量、坏 JSON、未知字段/版本、非法或缺失秘密、缺模板、空 package、非法 action、路径逃逸、错误 run/net/mode                                                  | 自包含脚本和 transport 均失败关闭；新 DV 还对正确非空字段和值做对象级断言，避免只验证负例，未发现输入边界缺陷                                                                         | pass   |
| state-and-data-integrity      | PreparedExecution、本地暂存、每步骤 workspace/context/template/package/file-secret 及脚本原子发布            | `src/execution.ts:25-65,74-192,413-513`；`src/transport.ts:256-352,458-489`；app/MySQL/Redis 脚本；rich-input DV                                          | 检查重复 close、部分上传、file-secret 持久副作用、模板/package 映射错位、artifact cleanup、配置/JAR/Redis 回退和 MySQL marker 重试                                      | context 与 package 路径按步骤 index 绑定，configure 外 secrets 为空；成功及 context 上传失败均关闭 artifact/local/workspace/session，脚本各自保持原子或回退边界，未发现数据完整性反例 | pass   |
| error-handling-and-recovery   | 上传、预检、脚本非零、取消、清理失败、服务配置和 App 回退                                                    | `src/execution.ts:295-367,399-527`；`src/transport.ts:282-352,458-489,527-612`；DV fail-fast/cancel/upload-failure；App deploy 与 Redis configure         | 检查上传异常是否继续执行、取消是否连接后续目标、cleanup 是否遮蔽主错，以及发布/服务失败是否恢复旧文件                                                                   | 强制 context 上传失败产生 FAILED+target-fail-fast、零 execute，并完成 context/workspace/session/local/artifact 清理；取消和脚本非零分类仍正确，未发现吞错或无界恢复                   | pass   |
| resource-lifetime-and-cleanup | 本地固定输入、下载 artifact、context、workspace/session 与 OpenSSH 子进程                                    | `PreparedExecution.close`、`#executeStep` 双层 finally、session close、`terminateAndReap`；DV close/cancel/upload-failure；transport timeout/cancel tests | 逐一追踪成功、脚本失败、上传失败、timeout、取消和重复 close 时的资源；检查清理是否错误携带 aborted signal                                                               | 本地/远端 context、workspace、session、artifact 与本地目录均有明确释放路径；上传失败回归精确断言清理事件，OpenSSH 取消/超时会终止并 reap，未发现遗留                                  | pass   |
| concurrency-and-ordering      | 串行步骤、取消/清理顺序、运行时预检缓存与跨目标隔离                                                          | `src/execution.ts:245-367,391-405,490-513`；DV strict event list、in-flight cancellation；transport 同步取消测试                                          | 挑战 context/package/script 顺序、步骤间 workspace 生命周期、同机预检次数、factory 同步取消窗口和失败目标对其它目标的影响                                               | rich-input DV 对完整事件列表做严格相等，取消回归只执行/连接一次且关闭资源，transport 在成功返回前复查 signal 并 reap 子进程；未发现竞态或排序阻断                                     | pass   |
| interface-and-compatibility   | 公开 `PreparedStep`/`PreparedExecution`、普通 context wire contract、删除的 runtime generator 及历史迁移边界 | `src/execution.ts:17-65`、`src/mod.ts:35-43,73-81`、positive/negative contract、consumer closure、三份说明                                                | 编译当前字段消费者，确认旧 `runtimeFile(s)` 被拒绝，扫描 helper 符号，并核对本地 `DeploymentContext` 与远端注入的边界                                                   | breaking 字段删除有双向 contract；本地 API 仍可作为控制端导出但未被上传，旧 helper 脚本不提供 shim 的迁移说明与 proposal 一致                                                         | pass   |
| security-and-capacity         | context/脚本权限、秘密最小选择与脱敏、Deno capability、路径/命令注入和临时容量                               | `src/remote_context.ts:57-155`、`src/execution.ts:107-149,421-461,495-526`、`src/transport.ts:256-305,418-455,639-735`；rich-input DV；OpenSSH 0600 unit  | 检查秘密是否进入 argv/日志、非 configure 是否收到秘密、context/template/package/script mode 是否放宽、remote/npm/ffi/env/run/net/path 是否越权                          | 新 DV 精确断言 configure 仅收到选中秘密、deploy secrets 为空、数据 0600/脚本 0700；OpenSSH unit 验证默认 mode 形成真实 chmod 0600，固定 Deno 权限与 Redactor 未发现暴露或绕过         | pass   |
| test-adequacy                 | contract/unit/DV/integration 是否能暴露正常、边界、错误、生命周期、兼容、安全和跨模块回归                    | `testplan.yaml`、当前测试源码、新 8-step artifact、本轮独立 DV 7/unit 8/integration 4/closure/docs 运行                                                   | 逐项验证漏传 secret、template key 错配、package_path 缺失、file-secret 未投递、mode 放宽、顺序改变、execute 参数偏移、上传失败不清理、helper 重现时是否有失败断言       | A019-F-001 已由对象/事件全等断言、OpenSSH chmod 断言与失败注入闭合；真实 SSH/systemd/MySQL/Redis 仍是具体人工环境 gap，未被自动测试冒充，未发现新的充分性阻断                         | pass   |

## Document Consistency

| Document | Source                                                   | Implementation Consistency                                                                                                                                                        | Finding                                                                                                      | Status |
| -------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------ |
| design   | `pipeline/plan.md`、`risk-profile.yaml`                  | 当前实现遵循普通 JSON、零框架源码、声明脚本上传、最小权限、逐步骤 workspace、失败清理和迁移型兼容边界；模板与实际集群消费者闭合                                                   | Key Call Flow、State Ownership、Failure Flows 与生产代码及新严格事件断言一致，未发现设计或实现偏离           | pass   |
| testing  | `testplan.yaml`、runtime `testing_evidence`、新 artifact | testplan 当前明确列出真实 prepareExecution、非空 secrets/templates/package/file-secret、有效 mode、严格顺序及上传失败清理；DV/transport unit 的具体断言和新 artifact 命令与之对应 | 第一轮文档高估覆盖的问题已消除；真实目标机与 OS 服务分支以具体 owner/reason/acceptance impact 保留为人工 gap | pass   |

## Result Summary

- Overall result: accepted
- Outcome: 第二轮从当前主源重新证伪后，A019-F-001 的每个原反例均有具体、可失败的跨模块或 OpenSSH
  命令断言；零远端框架源码、独立脚本普通输入、权限、顺序、失败与清理边界达到已批准 proposal。
- Blocking issues: 无；本轮未发现新的需求、设计、实现或测试阻断。真实 SSH 与实际 systemd/MySQL/Redis
  操作仍按 testplan 保留为发布环境人工验收，不被本报告视为已自动证明。
- Next action: 父级自动流水线可记录 A-1 accepted，并完成 runtime state、stage
  scope、pipeline/lifecycle 和任务索引收尾；不得把本报告或 checker pass
  描述为未来不存在任何缺陷的证明。

## Conclusion

- Accepted / rejected / needs changes: accepted
- Reason: 独立证伪覆盖唯一 change_id、A019-F-001
  的全部原反例和十个必查类别；当前实现、消费者、测试设计、具体断言、新任务制品与本轮独立运行没有留下阻断
  finding，且 design/testing 文档与实际行为一致，因此可以在已声明的真实目标机人工 gap 边界内接受。
