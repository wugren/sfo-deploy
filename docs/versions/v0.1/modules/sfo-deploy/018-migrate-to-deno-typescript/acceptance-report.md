# sfo-deploy Deno TypeScript 迁移第四轮独立验收报告

## Findings

| ID         | Severity | Owning Stage | Correctness Category        | Evidence                                                                                                                                                                                                                         | Problem                                                                                                        | Blocking |
| ---------- | -------- | ------------ | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | -------- |
| A018-F-001 | none     | none         | interface-and-compatibility | `src/downloads.ts:265-341` 的公开 `FilehubDownloadProvider.fetch` 在发布前调用 `verifyArtifact`，失败路径按 inode 身份清理；`tests/unit/downloads_secrets.test.ts:136-264` 的错误哈希、容量、metadata 与 no-publish 断言本轮通过 | 已关闭：direct provider 不再绕过哈希与容量合同，也不返回空的校验元数据                                         | no       |
| A018-F-002 | none     | none         | state-and-data-integrity    | `src/history.ts:824-869`、`1746-1783` 和 `1879-1888` 通过 `writeAll` 处理短写并在 sync 后发布；短写与零进展回归本轮通过                                                                                                          | 已关闭：发布历史的归档和原子 JSON 写入不会把部分字节发布为有效状态                                             | no       |
| A018-F-003 | none     | none         | concurrency-and-ordering    | `src/history.ts:1799-1867` 只允许同 dev/ino 的 `nlink=2` 发布窗口有界收敛，并拒绝持久双链接、`nlink>2` 与身份替换；四项 nlink/identity 断言本轮通过                                                                              | 已关闭：合法 hard-link 发布窗口可读，其它链接或替换状态继续失败关闭                                            | no       |
| A018-F-004 | none     | none         | test-adequacy               | `tests/unit/history_regressions.test.ts` 的磁盘 v1/v2/v3 fixture、短写、nlink、完整性与 rollback，以及 transport timeout/cancel、DV cleanup 均可执行；本轮 unit 35、DV 4、integration 4 全部通过                                 | 已关闭：第一轮指出的下载、历史、回退、超时与清理验证缺口已有能在旧实现失败的回归                               | no       |
| A018-F-005 | none     | none         | concurrency-and-ordering    | `src/downloads.ts:388-440` 在校验和创建 `.provider-*` 目录前检查 signal，并在内层 provider 返回、复验、取身份、hard-link、清理和返回前逐边界复查；catch 按 staged inode 清理本次已发布目标                                       | 已关闭：registry pre-abort 不创建暂存，provider-return 或 hard-link 后同步取消均不会发布并成功返回             | no       |
| A018-F-006 | none     | none         | test-adequacy               | `tests/unit/downloads_secrets.test.ts:315-464` 直接计数 `Deno.makeTempDir` 为零，在 provider return 内同步 abort，并在 `Deno.link` syscall 后 abort；分别断言拒绝、artifact cleaned、最终目标不存在和 staging 不残留             | 已关闭：registry 的 no-staging、provider-return no-publish 与 owned hard-link cleanup 都有确定性回归           | no       |
| A018-F-007 | none     | none         | concurrency-and-ordering    | `src/transport.ts:527-575` 注册 abort listener 后立即补查 `signal.aborted`，`Promise.race` 成功后再次补查；所有取消异常进入 `terminateAndReap`，先 SIGTERM、必要时 SIGKILL，并等待 `outputPromise`                               | 已关闭：`commandFactory` 内同步 abort 不再返回成功，已启动的 OpenSSH 子进程会终止并回收后抛出 `CancelledError` | no       |
| A018-F-008 | none     | none         | test-adequacy               | `tests/unit/transport_cli.test.ts:89-121` 在 command factory 同步调用 `controller.abort()`，以 pending output 观察 kill 和 reap，明确断言 `CancelledError`、factory 一次、`killed=true`、`reaped=true`；本轮通过                 | 已关闭：测试不再只覆盖 listener 注册后的 microtask 取消，已覆盖 spawn/factory 注册窗口                         | no       |

## Object and Scope

- Task manifest: task.yaml
- Risk profile: risk-profile.yaml
- Review date: 2026-09-01
- In-scope implementation: `deno.json`、`deno.lock`、`src/*.ts`、示例 TypeScript
  消费者、README/配置指南、`tests/**/*.ts`、任务级统一测试入口，以及
  `.harness/test-results/test-runs/20260901T140415Z-sfo-deploy+018-migrate-to-deno-typescript-all.json`
- Review mode: independent falsification; conclusion selected after findings and category
  review；本轮未参与实现或测试，直接检查提案、计划、当前生产调用链与测试断言，独立重跑
  unit/DV/integration，再逐项复核 A018-F-001 至 A018-F-008，而不采纳第三轮结论

## Requirement Coverage

| change_id                    | Requirement or Boundary                                                                                    | Source                                                                                 | Implementation Evidence                                                                                                                                                                                                                            | Finding                                                                                                         | Status |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------ |
| CHG-deno-package-entry       | 提供 Deno 原生公开入口，并以最小权限类别全局安装为 PATH 中的 `sfo-deploy`                                  | `proposal.md` P-001、Success Criteria                                                  | `deno.json` 导出与锁文件、`src/mod.ts`、`src/cli.ts`/`src/main.ts`；最新 artifact 的新路径编译和文档合同 exit 0，本轮 `tests/integration/package_cli.test.ts` 又在隔离安装根完成安装、PATH `--help` 与卸载                                         | 本地路径安装与固定 tag 文档边界符合提案；没有执行或声称真实注册表发布                                           | pass   |
| CHG-typescript-control-plane | TypeScript 等价提供配置、规划、下载、秘密、OpenSSH、执行、结果、历史与回退，并在错误、超时和取消时失败关闭 | `proposal.md` P-002、Scope/Risks；`risk-profile.yaml` contract/data/security/runtime   | `src/config.ts`、`planning.ts`、`downloads.ts`、`secrets.ts`、`transport.ts`、`execution.ts`、`history.ts`、`integration.ts`；本轮检查 registry/OpenSSH 竞态源码并重跑 unit 35、DV 4，全部通过                                                     | A018-F-001、002、003、005、007 的实现缺陷均已关闭；未发现新的控制平面阻断                                       | pass   |
| CHG-deno-consumer-migration  | 仓库内产品与示例改用 Deno API，移除 Python 产品运行时和构建依赖，同时保留 Harness Python                   | `proposal.md` P-003、Scope/Out of scope；`pipeline/plan.md` Consumer Migration Closure | Python 产品包和根/示例 Python 构建入口已移除；`examples/eleph-server-multipass/deno.json` 与 `src/cli.ts` 消费 `src/mod.ts`；artifact 的 old-path、removed-symbol、repository closure、docs 五项 contract 均 exit 0，本轮示例 integration 2 项通过 | 未发现仓库内产品或示例仍运行 Python `sfo_deploy`；历史 v1 Python 脚本只读回退与 Harness Python 属于明确保留边界 | pass   |
| CHG-deno-verification        | Deno unit/DV/integration、安装冒烟、兼容路径与任务级入口应覆盖四个 change_id 和关键失败路径                | `proposal.md` P-004、Success Criteria；`testplan.yaml`                                 | 最新 artifact 精确绑定四个 change_id，记录 5 contract、unit、DV、integration 共 8 steps 全部 exit 0；本轮独立重跑 unit 35、DV 4、integration 4，新增三项 registry 与一项同步 OpenSSH 取消断言均通过                                                | A018-F-004、006、008 的验证缺口已关闭；真实 SSH 部署仍按 testplan 的具体 manual gap 保留，不被自动证据冒充      | pass   |

## Independent Defect Discovery

| Category                      | Applicable Scope                                                                                    | Evidence Inspected                                                                                                                              | Adversarial Check                                                                                                                  | Finding or Not-Applicable Reason                                                                                                    | Status |
| ----------------------------- | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------ |
| requirement-and-behavior      | Deno 包/命令、控制端等价迁移、消费者迁移与验证闭环                                                  | `proposal.md` 四项 Proposal Items、Success Criteria、Non-goals，`pipeline/plan.md` acceptance baseline/failure flows，当前代码和测试            | 分开挑战安装、公共 API、十一项 CLI 动作、旧 snapshot、Python 移除，以及八个历史 finding 是否真实关闭                               | 四个 change_id 均有当前实现与可执行证据；未把注册表发布或真实目标机部署误计入仓库内完成范围                                         | pass   |
| logic-and-control-flow        | 配置规划、provider/registry 校验发布、OpenSSH 等待、执行 fail-fast 与历史读写                       | `loadCluster`/`buildPlan`、`FilehubDownloadProvider.fetch`、`DownloadProviderRegistry.fetch`、OpenSSH `#runLocal`、executor loop、history codec | 挑战未知 YAML、错误 hash/容量、pre-abort、provider return、link syscall、factory/listener、短写、目标失败和 rollback 分支          | registry 与 OpenSSH 的成功路径都在最终返回前复查取消；规划、执行与历史分支的本轮断言未暴露错误控制流                                | pass   |
| boundary-and-input            | YAML/路径/URL/hash/容量/AbortSignal、CLI argv、历史 JSON/codec/文件身份                             | `src/config.ts` 严格解析、download request/provider、transport argv、history `readRegularBytes`/fixtures、CLI parser                            | 挑战重复/未知字段、路径逃逸、凭据 URL、错误 hash、`maxBytes`、预取消及发布窗口、额外/缺失/symlink、非法参数                        | malformed/极值输入失败关闭；registry pre-abort 的 `makeTempDir=0` 与两个发布取消边界均由具体断言覆盖                                | pass   |
| state-and-data-integrity      | 下载 staging/destination、发布 intent/outcome/snapshot/lineage、no-clobber 与消费者状态             | registry 的 artifact ownership、`publishWithoutReplace`、`writeAll`/`atomicJson`、`verifySnapshot`、rollback attempt                            | 在 provider 返回及 hard-link 后取消，注入短写/zero progress、文件身份替换、manifest/outcome/lineage 篡改和重复发布                 | hard-link 后取消只删除同 inode 的本次目标；历史短写、篡改、lineage 和来源快照不可变性检查均通过                                     | pass   |
| error-handling-and-recovery   | 下载、SSH、执行、发布历史的错误、超时、取消与部分失败恢复                                           | Filehub/registry catch、OpenSSH `#runLocal`/`terminateAndReap`、executor finally、PreparedExecution.close、PendingRelease 终态                  | 挑战错误 hash、filehub 非零与取消、OpenSSH timeout/同步取消、目标内失败、跨目标继续、清理异常和 rollback                           | 下载不发布可疑工件，OpenSSH 取消 kill/reap 后分类为取消，目标隔离与发布终态保持可用；未发现吞错或错误成功                           | pass   |
| resource-lifetime-and-cleanup | provider 暂存、owned destination、OpenSSH 子进程/session/workspace、PreparedExecution、历史锁与文件 | downloads identity cleanup、transport `terminateAndReap`/session.close、executor finally、PreparedExecution.close、OperationLock.release        | 观察 pre-abort 是否获取目录/进程，provider-return/link 后是否残留，timeout/cancel 是否 kill/reap，重复 close 是否安全              | registry pre-abort 零目录，取消后无目标/staging；同步 OpenSSH 取消确认 kill 与 reap；DV 覆盖远端/本地资源清理                       | pass   |
| concurrency-and-ordering      | 取消监听/成功完成竞态、hard-link no-clobber/history 收敛、发布锁与目标执行顺序                      | registry 各取消检查、OpenSSH listener 与返回检查、history nlink/OS lock、executor sessions/failedMachines                                       | 在 provider return、link syscall、command factory 内同步取消，并挑战 nlink=2/3/身份替换、同集群锁、目标 fail-fast                  | 三个最新竞态探针均得到取消与清理；history 只收敛合法双链接窗口，锁及跨目标顺序未发现阻断                                            | pass   |
| interface-and-compatibility   | `src/mod.ts` 导出、CLI/JSON/YAML、DownloadProvider/Transport、示例消费者与 v1/v2/v3 codec           | `deno.json`/`mod.ts`、CLI serializer/exit mapping、公开 interfaces、磁盘 fixtures、示例 import 与 contract artifact                             | 编译新 TypeScript 路径、拒绝旧 Python 产品路径、PATH 调用命令、读取三代 snapshot，并以公开 signal 参数触发 registry/transport 取消 | Deno API 和仓库内消费者闭包通过；CLI/YAML/JSON 与历史 codec 保持，Python API 的破坏性迁移按设计明确                                 | pass   |
| security-and-capacity         | Deno 权限、known_hosts/argv、秘密脱敏、下载与历史容量、路径和反序列化边界                           | README 安装命令、OpenSSH strict options/quote、`ProjectBindings`/`Redactor`、download hash/maxBytes、history byte/item/depth 限制               | 检查是否使用 `-A`、shell 拼接或宽松 host key；挑战秘密输出、路径/链接、错误 hash、超大/深层 JSON 和未登记 snapshot 文件            | 安装限制 run 为 ssh/scp/filehub，OpenSSH 使用 argv 与严格 known_hosts；秘密、路径、hash 和容量均失败关闭，未发现新泄露或放大阻断    | pass   |
| test-adequacy                 | contract/unit/DV/integration、安装、registry 竞态、codec/rollback、timeout/cancel/cleanup           | `testplan.yaml`、相关测试断言、最新 8-step artifact 与本轮 unit 35/DV 4/integration 4 输出                                                      | 把每项 coverage 映射到会在旧错误实现失败的断言，重点验证 staging 创建计数、provider-return/link 和 factory 同步取消窗口            | A018-F-006/A018-F-008 已变为确定性回归；四个 change_id、七类 runtime coverage 和安装闭环有可执行证据，真实 SSH 缺口记录具体且不虚报 | pass   |

## Document Consistency

| Document | Source                                      | Implementation Consistency                                                                                                                                       | Finding                                                                                      | Status |
| -------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------ |
| design   | `pipeline/plan.md`、`risk-profile.yaml`     | 当前模块边界、OpenSSH argv、下载所有权、持久 codec、安装和消费者闭包遵循计划；取消进入统一清理且不提交成功副作用                                                 | 第三轮 registry/OpenSSH 竞态已经按原设计修复，不需要扩大或修改设计边界                       | pass   |
| testing  | `testplan.yaml`、runtime `testing_evidence` | unit summary/notes 已准确列出 no-staging、provider-return/link cleanup 和同步 factory kill/reap；最新 artifact 绑定相同 testplan、四个 change_id 与 8 个成功步骤 | 测试声明与当前断言、最新制品和本轮独立重跑一致；真实 SSH gap 的边界与 acceptance impact 具体 | pass   |

## Result Summary

- Overall result: accepted
- Outcome: A018-F-001 至 A018-F-008 均由当前生产代码、确定性回归和最新任务制品关闭；Deno
  包/命令、TypeScript 控制端、消费者迁移、历史与安全基线达到已批准提案。
- Blocking issues: 无；本轮未发现可确定复现的需求、设计、实现或测试阻断，真实 SSH 部署仅保留为
  testplan 已声明的人工环境缺口。
- Next action: 父级自动流水线可将 A-1 记录为 accepted，完成 runtime state、pipeline/lifecycle
  与任务索引收尾；不得把本报告描述为不存在任何未来缺陷的证明。

## Conclusion

- Accepted / rejected / needs changes: accepted
- Reason: 独立证伪覆盖全部四个 change_id 和十个必查类别，第三轮的 registry
  pre-abort/provider-return/hard-link 与 OpenSSH
  同步取消缺陷及测试缺口均已用当前源码和可失败回归关闭，最新任务 artifact
  与本轮独立执行也完整通过，因此没有剩余阻断。
