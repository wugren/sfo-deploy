schema_version: 1
task_manifest: task.yaml
change_ids: ["CHG-001", "CHG-002", "CHG-003"]
source_bindings:
  proposal: proposal.md
  design_source: design.md
risks:
  contract:
    applies: true
    evidence:
      - "design.md File-Level Interfaces 与 Consumer Migration Closure 记录 app schema 1、旧版本拒收和 configs/management 契约"
    required_checks: ["config-loader-schema-tests", "app-lifecycle-contract-tests"]
  data:
    applies: true
    evidence:
      - "design.md File-Level Interfaces 记录内部配置归一；history 需处理新管理声明"
    required_checks: ["history-compatibility-tests", "config-loader-schema-tests"]
  security:
    applies: true
    evidence:
      - "design.md Key Flows 记录 run_as、权限白名单、秘密隔离、失败关闭和补偿边界"
    required_checks: ["app-lifecycle-contract-tests", "managed-transport-security-tests"]
  runtime:
    applies: true
    evidence:
      - "design.md Key Flows 与 Risks and Rollback 记录文件/脚本配置、system/script 服务、Ubuntu/CentOS 工具与失败行为"
    required_checks: ["app-management-execution-tests", "cross-distro-service-tests"]
  build:
    applies: true
    evidence:
      - "design.md API and Build Surface Impact 与 Consumer Migration Closure 记录破坏性配置和文档示例迁移"
    required_checks: ["deployment-bundle-tests", "config-loader-schema-tests"]
  ui:
    applies: false
    evidence: 无前端或导航界面变更。
    required_checks: []
  harness:
    applies: true
    evidence:
      - "design.md Implementation Order 与 File-Level Implementation Sequence 记录 staged lifecycle 与文件级交付"
    required_checks: ["targeted-test-suite"]
