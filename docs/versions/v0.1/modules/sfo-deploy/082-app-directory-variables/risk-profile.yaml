schema_version: 1
task_manifest: task.yaml
change_ids: ["CHG-app-directory-variable-contract"]
source_bindings:
  proposal: proposal.md
  design_source: design.md
risks:
  contract:
    applies: true
    evidence: ["src/types.ts", "src/config.ts", "src/history.ts", "README.md", "docs/guides/sfo-deploy-cluster-configuration.md", "docs/modules/sfo-deploy.md", "skills/sfo-deploy-cluster/references/app.md"]
    required_checks: ["app-directory-variable-schema-contract", "plan-snapshot-directory-variable-contract"]
  data:
    applies: false
    evidence: 不新增持久化状态；变量只在装载期归一为计划内只读定义并随计划快照编码。
    required_checks: []
  security:
    applies: true
    evidence: ["src/config.ts", "src/execution.ts", "src/transport.ts"]
    required_checks: ["app-directory-variable-path-boundary"]
  runtime:
    applies: true
    evidence: ["src/execution.ts", "src/transport.ts"]
    required_checks: ["versioned-config-directory-runtime-contract"]
  build:
    applies: false
    evidence: 不改变构建产物、依赖图或包发布面。
    required_checks: []
  ui:
    applies: false
    evidence: 不涉及用户界面、导航或无障碍变更。
    required_checks: []
  harness:
    applies: false
    evidence: 任务工件使用现有 Harness，不改变流程或检查器行为。
    required_checks: []
