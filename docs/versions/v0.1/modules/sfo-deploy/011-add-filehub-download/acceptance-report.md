# filehub 下载支持第二轮独立验收报告

## Findings

| ID    | Severity | Owning Stage | Correctness Category | Evidence                                                                                                                                                                       | Problem                                                                                         | Blocking |
| ----- | -------- | ------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- | -------- |
| F-000 | none     | none         | overall              | 重新检查当前 `downloads.py`、README、两份下载测试、新任务运行制品及上游 commit `1931c789f4733d17f6455c22b0aa9af2eb873120`，并独立重放 no-clobber、最终 wait 超时与清理失败反例 | 未发现仍可复现的需求、设计、实现或验证阻断缺陷；真实 filehub 服务联调仍是已声明的发布前人工风险 | no       |

## Object and Scope

- Task manifest: task.yaml
- Risk profile: ./risk-profile.yaml
- Review date: 2026-08-31
- In-scope implementation: `src/sfo_deploy/downloads.py` 的 filehub
  provider、注册表、子进程和工件生命周期，`README.md`，`tests/unit/test_filehub_downloads.py`、`tests/unit/test_downloads.py`，测试制品
  `.harness/test-results/test-runs/20260831T054302Z-sfo-deploy+011-add-filehub-download-all.json`，以及
  `/tmp/tmp.fF05T2oZF0/sfo-filehub` 的官方 CLI 契约
- Review mode: independent falsification; conclusion selected after findings and category review;
  this round re-established evidence from current primary sources without adopting the prior
  acceptance conclusion

## Focused Reverification

| Prior ID | Current production evidence                                                                                                                                                                              | Independent adversarial observation                                                                                                                                | Current registered test evidence                                                                                                                                       | Result |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| F-001    | `FilehubDownloadProvider.fetch` 在 `src/sfo_deploy/downloads.py:269-305` 记录 staged inode，并以 `os.link(..., follow_symlinks=False)` 原子创建不存在的 destination；`FileExistsError` 映射为拒绝覆盖    | 在 `os.link` 入口先创建内容为 `concurrent-owner` 的 destination，观察到 `DownloadError("下载目标已存在，拒绝覆盖")`，最终字节仍为 `concurrent-owner` 且暂存树清除  | `tests/unit/test_filehub_downloads.py:307-339` 把竞争创建精确放在 link 系统调用入口，断言不覆盖普通文件；`:262-290` 同时覆盖既存普通文件和符号链接                     | closed |
| F-002    | `_terminate_and_reap` 在 `src/sfo_deploy/downloads.py:489-504` 的 terminate 后 wait 与 kill 后 wait 均使用 `_FILEHUB_TERMINATE_TIMEOUT`，第二次超时抛有界 `DownloadError`                                | 永不退出的 fake process 产生事件 `terminate -> wait(2.0) -> kill -> wait(2.0)` 后立即报“强制终止后仍未退出”，没有 `wait(None)`                                     | `tests/unit/test_filehub_downloads.py:363-411` 覆盖正常 kill/reap 与最终 wait 再超时，并断言每次 wait 都有 timeout                                                     | closed |
| F-003    | `FilehubDownloadProvider.fetch` 的单一 `except BaseException` 在 `src/sfo_deploy/downloads.py:302-306` 调用 `_cleanup_filehub_staging`；后者在 `:628-634` 只把 OSError 作为 note 加到 primary 后原样重抛 | child wait 抛 `KeyboardInterrupt("original cancellation")` 且 staging cleanup 抛 OSError 时，最终类型仍为同一 `KeyboardInterrupt`，原消息保留并带具体 cleanup note | `tests/unit/test_filehub_downloads.py:413-448` 同时参数化 KeyboardInterrupt 与 RuntimeError，断言对象身份、子进程回收顺序及清理 note                                   | closed |
| F-004    | `testplan.yaml:43-56` 的 DV 注册现在直接运行完整 `tests/unit/test_downloads.py`，并保留 filehub DV；unit 注册包含上述竞态、最终 timeout 和 cleanup failure 用例                                          | 逐个把当前断言映射到 F-001/F-002/F-003 的失败机制，均会在旧行为回归时失败，而非只检查成功状态                                                                      | `20260831T054302Z...json:54-138` 记录 contract、unit、DV 三步 exit 0；DV argv 包含整个 `tests/unit/test_downloads.py`，覆盖其中 HTTP/hash/capacity/custom/URL 全部回归 | closed |

## Requirement Coverage

| change_id            | Requirement or Boundary                                                                                                                                       | Source                                                                                                                 | Implementation Evidence                                                                                                                                                                                                                                        | Finding                                                    | Status |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ------ |
| CHG-filehub-download | 内置 `provider: filehub`、严格 `source.target`、无 shell pull、缺失命令安装提示、安全失败/no-clobber、普通文件/大小/哈希二次校验、README 与既有 provider 兼容 | `proposal.md` P-001 至 P-004、Success Criteria、Risks；`./risk-profile.yaml` contract/security/runtime required checks | `src/sfo_deploy/downloads.py:232-378,438-512,579-670` 实现 provider、原子 no-clobber、固定诊断、有界进程回收、所有权清理和 registry 复验；`README.md:106-128` 提供配置/安装说明；独立探针与 `20260831T054302Z...json` 证实关键失败边界和完整既有 provider 回归 | 当前交付覆盖全部提案项；未发现缺失、收窄或超出非目标的行为 | pass   |

## Independent Defect Discovery

| Category                      | Applicable Scope                                                                       | Evidence Inspected                                                                                                                                                                                                       | Adversarial Check                                                                                                                                   | Finding or Not-Applicable Reason                                                                                                                                     | Status |
| ----------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| requirement-and-behavior      | P-001 至 P-004 的 provider、安装提示、安全失败、二次校验、文档和非目标                 | `proposal.md:19-70`、`./risk-profile.yaml:9-49`、`README.md:106-128`、`src/sfo_deploy/downloads.py:232-378`                                                                                                              | 从合法 filehub 配置、命令缺失、非零退出、目标并发出现、校验失败和现有 provider 不变逐项寻找反例                                                     | filehub 主流程、明确安装提示、no-clobber、失败清理、最终校验与文档均有当前实现和可失败测试；需求无歧义或遗漏                                                         | pass   |
| logic-and-control-flow        | source 校验、Popen/wait、退出码分派、staged 检查、发布、清理和 registry 复验           | `FilehubDownloadProvider.fetch`、`_run_filehub_pull`、`_terminate_and_reap`、`DownloadProviderRegistry.fetch`、`_verify_artifact`                                                                                        | 追踪 0、1-8、未知非零、无输出、非普通文件、link EEXIST/OSError、timeout、BaseException 和复验异常分支                                               | 每条失败路径在返回前清理或记录清理失败；成功只能在原子发布、暂存清除和 registry 二次校验后返回，无错误 fallthrough 或无界 wait                                       | pass   |
| boundary-and-input            | `source.target` 类型/空白/未知字段、destination 父目录/既存类型、staged 类型和数值限额 | `src/sfo_deploy/downloads.py:235-275,387-448,579-614`、`tests/unit/test_filehub_downloads.py:87-149,224-361`、`tests/unit/test_downloads.py:63-96`                                                                       | 检查空/非字符串/额外 config、缺父目录、普通文件/符号链接 destination、目录/符号链接 output、max_bytes 和坏 hash                                     | 同步输入边界在启动/发布前拒绝；filehub 目标作为单 argv 交给官方四段 parser；异常产物与超限/哈希错误不能进入部署                                                      | pass   |
| state-and-data-integrity      | staging 目录、staged inode、destination、`VerifiedArtifact` 与清理所有权               | `pipeline/plan.md:154-170`、`src/sfo_deploy/downloads.py:246-306,637-670`、`tests/unit/test_filehub_downloads.py:475-581`                                                                                                | 在发布系统调用入口创建竞争 destination，并在已发布后的 staging 清理失败点把 destination 替换为另一 inode                                            | 原子 link 不覆盖竞争目标；失败清理以 dev/inode 核对所有权，测试证明已被替换的 destination 保留，hash/max_bytes 失败删除仍属本次下载的工件                            | pass   |
| error-handling-and-recovery   | 命令缺失、启动 OSError、退出码、timeout、取消、发布/暂存/目标清理失败                  | `src/sfo_deploy/downloads.py:250-306,458-512,621-670`、`tests/unit/test_filehub_downloads.py:115-222,341-471,519-559`                                                                                                    | 强制 FileNotFound、权限错误、各退出码、两级 wait 超时、KeyboardInterrupt 加 cleanup OSError、并发替换后 cleanup                                     | 缺失命令给出两平台安装入口；敏感底层详情不进入主错误；timeout 有界；清理失败以 note 保留且不掩盖 primary；非本 owner 的替换文件不删除                                | pass   |
| resource-lifetime-and-cleanup | filehub 进程、DEVNULL 标准流、0700 mkdtemp 私有树、上游 PID 临时文件和最终工件         | `src/sfo_deploy/downloads.py:246-306,458-504,621-670`、上游 `cli/src/cli/pull_handler.rs:20-28,74-124`                                                                                                                   | 对 success、非零、timeout/kill、取消、cleanup OSError、PID partial 和 registry 校验失败核对释放顺序                                                 | 所有 process wait 均有 900 或 2 秒期限；私有树覆盖上游同目录 PID partial；无法完成的清理被附注，正常/失败路径没有静默遗留可复用工件                                  | pass   |
| concurrency-and-ordering      | destination 的不存在前提、原子发布、发布后所有权和清理次序                             | `src/sfo_deploy/downloads.py:269-305,637-670`、`tests/unit/test_filehub_downloads.py:307-339,519-551`                                                                                                                    | 将竞争创建放入实际 `os.link` 入口，并在发布后清理异常时用 `os.replace` 换成另一 inode                                                               | hard-link 创建提供原子 no-clobber；EEXIST 保留竞争 owner 内容；后续清理按 inode 拒绝删除替换文件，未复现覆盖或重复 side effect                                       | pass   |
| interface-and-compatibility   | 官方 pull argv/目标/精确路径/退出码/临时文件/timeout 与 http/https/custom provider     | 上游 commit `1931c789...` 的 `cli/src/cli/pull_handler.rs:11-124`、`cli/src/cli/mod.rs:19-43,101-125`、`cli/src/apiclient/mod.rs:28-59,404-452`、`cli/src/archive/mod.rs:142-154`；本仓库 `downloads.py:309-378,458-512` | 对照 `filehub pull TARGET DESTINATION`、`.destination.pid.tmp`、0-8 退出码、reqwest 60 秒每请求期限和外层 900 秒总期限，并运行完整现有下载测试      | 当前 argv、输出路径与退出码映射匹配固定上游 commit；上游可覆盖行为被限制在私有目录；默认注册表仍保留 http/https，项目 custom provider 注入不变                       | pass   |
| security-and-capacity         | shell/argv、凭据与输出、路径类型、磁盘/时间/字节和哈希边界                             | `./risk-profile.yaml:22-37`、`src/sfo_deploy/downloads.py:37-49,232-306,458-512,579-618`、相关 security/runtime 单元与 DV 测试                                                                                           | 用特殊 target 核对单 argv 和 `shell=False`，检查三路 DEVNULL、固定诊断、symlink 拒绝、900 秒期限、max_bytes 与常量时间 hash 比较                    | 不执行 shell/安装脚本，不读取或输出凭据，原始 stdout/stderr 不泄露；发布产物必须是普通文件并接受大小及配置 hash 复验；未发现注入或无界进程等待                       | pass   |
| test-adequacy                 | contract、unit、DV、完整既有 provider 回归、任务制品与真实服务缺口                     | `testplan.yaml:10-62`、`tests/unit/test_filehub_downloads.py:1-621`、`tests/unit/test_downloads.py:1-96`、`20260831T054302Z...json`                                                                                      | 验证每个关键实现分支是否有能在回归时失败的断言，特别重放 no-clobber、最终 wait 超时、primary exception 身份、cleanup owner 和完整 HTTP/custom suite | 当前测试能暴露正常、边界、错误、生命周期、并发、兼容和跨模块失败；任务三步均成功。真实服务成功/认证失败/网络失败明确列为发布前人工 gap，不被自动测试或本结论冒充覆盖 | pass   |

## Document Consistency

| Document | Source             | Implementation Consistency                                                                                                                                                        | Finding                                                                                                              | Status |
| -------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------ |
| design   | `pipeline/plan.md` | 当前实现遵循 `./risk-profile.yaml` 与计划的 source、argv、私有 staging、原子 no-clobber、普通文件、900 秒总期限、有界 terminate/kill/wait、primary exception 和 registry 复验边界 | hard-link 后 unlink 是计划“同文件系统原子移动到仍不存在目标”的 no-clobber 实现；未发现设计缺陷或实现偏离             | pass   |
| testing  | `testplan.yaml`    | unit、DV 和 contract 注册与当前测试文件一致；DV 当前运行完整 `tests/unit/test_downloads.py`；运行制品绑定 CHG-filehub-download 和全部 evidence roots                              | `20260831T054302Z...json` 三步 exit 0 且命令与 testplan 一致；真实服务 gap 的 owner、原因和 acceptance impact 均明确 | pass   |

## Result Summary

- Overall result: accepted
- Outcome: 当前交付满足 filehub 内置 provider、安装提示、安全
  argv、no-clobber、失败恢复、有界进程生命周期、框架二次校验、文档和既有 provider
  兼容要求；四项重点反例均已通过独立重放和可失败测试关闭。
- Blocking issues: 无；本轮未发现阻断 finding。
- Next action: 可完成 acceptance；发布前仍由 `testplan.yaml` 指定的 sfo-deploy 发布负责人和隔离
  filehub 测试服务运维人员执行真实服务成功 pull、认证失败和网络失败三项人工验收。

## Conclusion

- Accepted / rejected / needs changes: accepted
- Reason: 重新从当前主源、固定上游
  commit、独立反例和当前任务运行制品完成十类证伪后，no-clobber、所有 wait
  有界、清理失败保留原异常以及完整既有 provider 回归均有直接证据，且没有剩余阻断缺陷。
