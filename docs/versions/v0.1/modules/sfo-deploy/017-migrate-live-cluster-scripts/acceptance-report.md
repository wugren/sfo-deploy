# 实时集群脚本迁移第二轮独立验收报告

## Findings

| ID    | Severity | Owning Stage | Correctness Category | Evidence                                                                                                                                                                                                                                                                                                                            | Problem                                                                                              | Blocking |
| ----- | -------- | ------------ | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | -------- |
| F-000 | none     | none         | overall              | 独立复查当前提案、风险、pipeline 设计、实时 cluster、规范模板、两类生成器、loader/planner、专用测试、受保护状态摘要和新任务制品 `20260901T105645Z-sfo-deploy+017-migrate-live-cluster-scripts-all.json`；另以临时目录直接复现无 live/无 evidence 与 synthetic live/无 evidence 两条守卫分支，并重新计算四个受保护文件的当前 SHA-256 | A017-F-001 与 A017-F-002 均已关闭；未发现需要返回 proposal、design、implementation 或 testing 的缺陷 | no       |

## Object and Scope

- Task manifest: task.yaml
- Risk profile: risk-profile.yaml
- Review date: 2026-09-01
- In-scope implementation: `examples/eleph-server-multipass/clusters/multipass/**`
  当前实时实例、`cluster-template/**`、`prepare-multipass.sh`、`prepare-multipass.ps1`、`src/sfo_deploy/config.py`、`src/sfo_deploy/planning.py`、相关
  Deno 执行/历史兼容消费路径、`tests/test_live_cluster_typescript_contract.py`、示例 TypeScript
  行为测试、`testplan.yaml`、`.harness/evidence/v0.1/live-cluster/017-migrate-live-cluster-scripts-protected-state.json`
  和新任务制品
- Review mode: independent falsification; 不继承上一轮 needs-changes 或 D/I/T 摘要，先从当前 primary
  sources 主动寻找反例，再形成结论

## Focused Reverification

| Prior Finding | Current Primary Evidence                                                                                                                                                                                                                                                              | Independent Adversarial Check                                                                                                                                                                                                                                                                                                                                                 | Result |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A017-F-001    | `_verify_protected_state` 在 evidence 缺失时仅要求 live cluster 列表为空；`test_missing_runtime_evidence_is_allowed_only_without_live_clusters` 同时构造零 live 正例与 synthetic live 负例；模板、专用测试及生成器路径不受 Git ignore，live `clusters/` 与 `.harness/` 运行态仍被忽略 | 未调用 pytest：在临时仓库根直接调用守卫，确认“无 live + 无 evidence”通过；再加入仅含 `cluster.yaml`/`machines.yaml` 的 synthetic live，确认以 `evidence is required` 失败关闭。`testplan.yaml` 的 `evidence_inputs` 仅列 `examples`、`tests`、`config.py`、`planning.py`，未把 `.harness` 作为 clean-checkout 输入；新制品 unit step 执行整个 `TestLiveClusterUnit` 且 exit 0 | closed |
| A017-F-002    | 受保护摘要绑定 `parent-orchestrator pre-I-1 read-only summary`，仅含四个文件的 SHA-256、七个非秘密机器字段和迁移前 `python: python3`；当前 live machine 为 `deno: /usr/local/bin/deno` 且无 `python`                                                                                  | 独立重算 `bootstrap.json`、`known_hosts`、私钥和公钥四个当前摘要，四项全部匹配；当前 machines 的 schema/name/private_ip/region/SSH user/port/key path 与摘要完全相同；扫描摘要与 run artifact 均未发现私钥、known_hosts 或 bootstrap 正文                                                                                                                                     | closed |

## Requirement Coverage

| change_id                      | Requirement or Boundary                                                                                                             | Source                                                                                                                                                 | Implementation Evidence                                                                                                                                                                                                                                     | Finding                                                                                                                | Status |
| ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------ |
| CHG-live-cluster-ts-migration  | 把实时集群 21 个 Python 生命周期脚本、五份资源 YAML 和 machine runtime 迁移到 TS/Deno，同时保持实例字段、秘密、权限、动作与恢复语义 | `proposal.md` P-001、Success Criteria；`pipeline/plan.md` File-Level Migration Map/State Ownership；`risk-profile.yaml` contract/data/security/runtime | 当前 live 有 21 个 `.ts`、无生命周期 `.py`，App/环境脚本与五份 YAML 合同逐项匹配模板；四个受保护文件摘要和七个机器字段匹配 pre-I-1 摘要；从 live 配置复制并只填入已声明的测试制品占位后，strict loader 成功且 deploy plan 为 14 个全 Deno/TS 步骤           | 未发现只改扩展名、部分 YAML 切换、模板整体覆盖、实例状态漂移或权限扩大                                                 | pass   |
| CHG-live-cluster-ts-regression | 提供仅针对可部署 live contract、同时能在 clean checkout 运行的防回退守卫，并证明两类生成路径持续消费 TS 模板                        | `proposal.md` P-002；`pipeline/plan.md` generation-boundary/regression-guard；`testplan.yaml` unit/DV/integration                                      | 可移植测试始终检查规范模板的 21 TS、五份 YAML 和机器 Deno；有 live 时追加 live/evidence 检查，无 live 时不依赖 ignored 状态；synthetic `.py` 与旧 `.py` YAML 负例失败关闭；shell/PowerShell 均直接复制当前模板；新任务制品三个实际命令 exit 0、无未执行层级 | ignored live 与 `.harness` 只增强本机迁移验证，不再是干净检出的必要输入；一般 Python、历史基线和兼容夹具仍在扫描边界外 | pass   |

## Independent Defect Discovery

| Category                      | Applicable Scope                                                                       | Evidence Inspected                                                                                                             | Adversarial Check                                                                                                                        | Finding or Not-Applicable Reason                                                                                                                                                                                                      | Status |
| ----------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| requirement-and-behavior      | 实时迁移、持续防回退、状态保护、权限非目标和实机边界                                   | `proposal.md` P-001/P-002、当前 live/template、生成器、专用测试和新 artifact                                                   | 分别挑战仅改文件名、仅改引用、整体模板覆盖、全仓禁止 Python、无 live 交付环境和把子进程误当完全沙箱                                      | 两个 change_id 均在批准边界内闭合；Deno 直接 I/O 限步骤工作区、默认禁网以及获准子进程不继承完整限制的边界保持不变，没有需求歧义或静默扩张                                                                                             | pass   |
| logic-and-control-flow        | live/template 发现、可选 evidence、loader/planner、生成复制和动作分派                  | `_live_clusters/_template_roots/_verify_protected_state/_stage_loadable_cluster`、`load_cluster/build_plan`、两类 prepare 入口 | 主动运行零 live 与 synthetic live 两条相反分支；检查有 live 时摘要失败关闭、deploy 依赖顺序及 start/stop/restart 权限选择                | clean 分支不再空列表失败，synthetic live 缺摘要必然失败；staged live 生成 14 步全 Deno/TS plan，三个生命周期动作选择正确脚本和 net 白名单                                                                                             | pass   |
| boundary-and-input            | `examples/**/clusters/**`、模板、YAML 对象、脚本路径、run/net、机器 runtime 与制品占位 | 专用 helper、严格 config parser、五份 live/template YAML、testplan 输入根                                                      | 挑战 clusters 外 baseline/fixture Python、旧 `.py` 引用、非法权限字段、缺 Deno、无 live、缺 evidence 和未经填充的示例制品占位            | 扫描只识别带 cluster/machines 声明的 live 目录；历史 Python 不误报；strict loader 拒绝 `.py`，测试只以非秘密测试值满足文档化制品前置，没有把占位替换伪装成生产发布成功                                                                | pass   |
| state-and-data-integrity      | bootstrap、known_hosts、私钥/公钥、节点/SSH 字段、machine runtime 和脚本/YAML 原子配对 | pre-I-1 受保护摘要、当前 live 文件、machines、21 个脚本字节和五份 YAML 比较                                                    | 重算四个 SHA-256 而不打印内容；比较七个机器保护字段；寻找摘要中的文件正文、Python 残留、模板/live 脚本集合差异和部分迁移                 | 四个摘要及所有保护字段完全匹配，摘要只含散列与非秘密字段；runtime 从记录的 Python 变为 Deno，非生命周期保护集合无差异，脚本与 YAML 未见部分状态                                                                                       | pass   |
| error-handling-and-recovery   | 缺摘要、旧合同、脚本非零、JAR 健康失败、MySQL marker、Redis 恢复和生成失败             | pipeline Failure Flows、strict loader 负例、当前 Deno 行为测试、prepare 原子发布测试                                           | 注入 synthetic live 缺 evidence、旧 `.py` 引用、120 秒健康截止、JAR 新版离线、MySQL 导入失败和 Redis 健康失败                            | 迁移/验证失败均可见且失败关闭；行为测试检查 JAR 回退、marker 不早发、Redis backup 恢复/restart 和秘密脱敏，未发现吞错或恢复顺序倒置                                                                                                   | pass   |
| resource-lifetime-and-cleanup | 生成 staging、Deno 行为临时目录、步骤工作区和 ignored 本机缓存                         | prepare staging/publish 测试、`tmp_path` 行为 harness、当前 live/template 目录、Deno executor cleanup 路径                     | 检查失败后 staging/临时文件、Redis candidate/backup、当前脚本目录的非 TS 缓存及 clean-checkout 交付差异                                  | 临时资源由测试或既有执行器清理；本机 ignored `__pycache__/*.pyc` 未被 YAML 引用且不会进入 clean checkout，不构成生命周期源码或本任务交付依赖                                                                                          | pass   |
| concurrency-and-ordering      | 单一实时实例树的原子 I-1、生成原子发布、部署步骤排序                                   | pipeline 串行依赖、File-Level Implementation Sequence、prepare publish 测试和 planner 拓扑逻辑                                 | 挑战并行覆盖同一 machines/YAML、部分迁移可见、重复生成和依赖乱序                                                                         | 本任务没有引入运行并发；单一 I-1 串行迁移共享实例状态，生成器使用 staging 后发布，plan 保持确定依赖顺序，未发现 race 或乱序写入                                                                                                       | pass   |
| interface-and-compatibility   | live YAML、严格 loader、Deno plan、模板/生成器和历史 v1 Python/v2 Deno/v3 plan 边界    | 当前 `config.py`、`planning.py`、`history.py`、`transport.py`、五份 YAML、machine 和生成器                                     | 核对新配置只接受 `.ts`/Deno，读取历史 decoder 是否仍保留 v1 Python 与 v2/v3 Deno，搜索 regression guard 是否越界清理兼容夹具             | live 消费闭包已迁移；核心历史 decoder 仍区分 v1 Python 与 v2/v3 Deno，017 未修改或误删该兼容层；生成器持续复制同一规范模板                                                                                                            | pass   |
| security-and-capacity         | Deno 直接权限、允许子进程例外、秘密输出、扫描范围和有界脚本集合                        | live/template 的 23 个动作调用、`execute_deno` 固定 argv、保护摘要 schema、测试事件与 artifact                                 | 搜索额外 net/run、裸 allow、远程/npm/FFI、目录外直接 I/O、秘密正文、通配扫描及意外扩大到全仓                                             | 23 个动作调用中 20 个 net 为空，唯一目标为既定 `127.0.0.1:8080`；run 与模板逐项一致；Deno 直接读写仍限步骤工作区且默认 deny-net/deny-run，获准子进程例外明确；证据不含秘密正文，21 脚本集合有界                                       | pass   |
| test-adequacy                 | 静态、行为、配置/计划、生成、防回退、保护状态和实机缺口                                | `testplan.yaml`、专用 unit/DV/integration、当前 TypeScript 行为测试、`deno_script_harness.ts`、新 artifact                     | 判断断言能否因无 live、synthetic live、`.py` 回退、权限漂移、保护文件变化、动作/恢复漂移而失败；核对实际运行命令、change_id 和未执行层级 | A017-F-001/F-002 的旧盲区已有正反例和摘要断言；新 artifact 绑定两个 change_id，contract/unit/DV 三个去重命令均 exit 0 且无 non-executed；真实 SSH/sudo/systemd/MySQL/Redis/JAR 仍未实机运行，但 testplan 明确负责人、风险和发布前影响 | pass   |

## Document Consistency

| Document | Source                                  | Implementation Consistency                                                                                                                                                                          | Finding                                                                                                                                       | Status |
| -------- | --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| design   | `pipeline/plan.md`、`risk-profile.yaml` | 当前 live 的 21 脚本、五份 YAML、machine Deno、受保护状态、生成边界及扫描作用域与设计映射一致                                                                                                       | 未发现设计自身缺陷或实现偏离；pre-I-1 摘要补齐设计已要求但上一轮未持久化的数据证据，不改变设计边界                                            | pass   |
| testing  | `testplan.yaml`                         | unit 明确 clean checkout 无 live/无 `.harness` 的行为，并在有 live 时要求摘要；DV 可选择 live 或模板；integration 检查两类生成器和 21 个 Deno check；新 artifact 的命令、来源、change_id 与登记一致 | `.harness` 不在 `evidence_inputs` 或 clean-checkout 前置；A017-F-001/A017-F-002 已关闭。manual real-target gap 准确披露，没有被自动化证据替代 | pass   |

## Result Summary

- Overall result: accepted
- Outcome: 当前实时 cluster 的 21 个 TS、五份权限 YAML、Deno
  machine、生成路径和受保护实例状态均通过独立证伪；可移植守卫在 clean checkout
  无运行态输入时通过，在 synthetic live 缺 evidence 时失败关闭；两个旧 finding
  已由当前代码、摘要和新任务制品关闭。
- Blocking issues: 无。真实隔离目标机上的 SSH、sudo、systemd、MySQL、Redis 与实际 JAR 发布仍是
  `live-cluster-real-target-execution`
  明示的发布前人工验证边界，本报告不把替身或离线检查表述为实机成功。
- Next action: 完成自动流水线 acceptance、生命周期和任务索引收尾；发布前由 testplan
  指定负责人执行隔离 Multipass 冒烟验证，若实际目标环境的二进制路径、sudo
  或服务语义不符则停止发布并返回实现/测试处理。

## Conclusion

- Accepted / rejected / needs changes: accepted
- Reason: 两个 change_id、十类缺陷发现和设计/测试一致性均通过；A017-F-001 的 clean-checkout
  依赖问题与 A017-F-002
  的前后状态证据问题都已由可失败的当前验证闭合，未发现新的阻断缺陷，剩余实机限制已明确保留为 manual
  gap。
